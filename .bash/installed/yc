
yc() {
    unset -f "${FUNCNAME[0]}"
    # already done in .bash/paths
    # export PATH=~/yandex-cloud/bin:${PATH}
    test -e ~/yandex-cloud/completion.bash.inc && source ~/yandex-cloud/completion.bash.inc
    ${FUNCNAME[0]} "$@"
}
