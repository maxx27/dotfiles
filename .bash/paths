
newpath=
paths=(
    ~/.local/bin
    ~/bin
    ~/.poetry/bin
    ~/yandex-cloud/bin
)
for d in ${paths[@]}; do
    if [ -d "$d" ] ; then
        newpath="$newpath$HOME/$d:"
    fi
done
if [ ! -z "$newpath" ]; then
    export PATH="$newpath$PATH"
fi
unset newpath paths
