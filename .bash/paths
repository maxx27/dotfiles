
# set PATH so it includes user's private bin if it exists
NEWPATH=
for d in .local/bin .poetry/bin bin; do
    if [ -d "$HOME/$d" ] ; then
        NEWPATH="$NEWPATH$HOME/$d:"
    fi
done
export PATH="$NEWPATH$PATH"
unset NEWPATH
